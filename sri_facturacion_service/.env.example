# Microservicio facturación SRI — SOLO PRUEBAS (celcer)
# El certificado .p12 NO va aquí: se pide en el modal de TecnoStock
# cada vez que se envía una factura (no se guarda en el backend).

PORT=5003

# Segundos de espera entre recepción y consulta de autorización
SRI_WAIT_AUTORIZACION_SEC=3
